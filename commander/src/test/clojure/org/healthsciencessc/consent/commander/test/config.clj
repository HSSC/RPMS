(ns org.healthsciencessc.consent.commander.test.config
  ;;(:use [org.healthsciencessc.consent.commander.config])
  (:use [clojure.test]))

;; Tests that the config is loaded as a map.
;;(deftest test-config
;;  (is (map? config)))
