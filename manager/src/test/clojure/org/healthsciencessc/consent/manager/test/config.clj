(ns org.healthsciencessc.consent.manager.test.config
  ;;(:use [org.healthsciencessc.consent.manager.config])
  (:use [clojure.test]))

;; Tests that the config is loaded as a map.
;;(deftest test-config
;;  (is (map? config)))
